import 'package:http/http.dart' as http;
import 'dart:convert';

class OpenAIService {
  final String _apiKey = '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';

  Future<String> gerarResposta(String prompt) async {
    final response = await http.post(
      Uri.parse("https://api.openai.com/v1/completions"),
      headers: {
        'Content-Type': 'application/json',
        'Authorization': 'Bearer $_apiKey',
      },
      body: jsonEncode({
        'model': 'text-davinci-003',
        'prompt': prompt,
        'max_tokens': 100,
      }),
    );

    if (response.statusCode == 200) {
      final json = jsonDecode(response.body);
      return json['choices'][0]['text'].toString().trim();
    } else {
      return 'Erro ao conectar com a IA.';
    }
  }
}
