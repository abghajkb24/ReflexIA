import 'package:flutter/material.dart';

class QuestionOfDayScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Tela para exibir a pergunta do dia
    return Scaffold(
      appBar: AppBar(
        title: Text('Pergunta do Dia'),
      ),
      body: Center(
        child: Text('Em breve: Pergunta do dia!'),
      ),
    );
  }
}
